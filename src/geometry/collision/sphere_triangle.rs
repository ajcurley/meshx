use crate::geometry::{Sphere, Triangle};

/// Check for a spatial intersection between an Sphere and Triangle
pub fn intersects_sphere_triangle(_sphere: &Sphere, _triangle: &Triangle) -> bool {
    unimplemented!()
}
